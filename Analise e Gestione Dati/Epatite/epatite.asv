clear all
close all

load("dati_malati.mat");

D = hcvdat1;

n = size(D,1);      % Numero di pazienti
f = size(D,2);      % Numero di features: età, malato etc

% Vorrei vedere l'istograma
% for i=1:f
%     figure
%     histogram(D(:,i));
% end


for i=1
figure
plot(D(:,1), D(:,3), 'o')
